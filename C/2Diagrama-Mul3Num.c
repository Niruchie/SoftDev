#include <stdio.h>

main(){
	int nx, ny, nz, mul = 0;

	printf("Digite tres numeros a multiplicar:\n");
	scanf("%d", &nx);
	scanf("%d", &ny);
	scanf("%d", &ny);
	mul = nx * ny * nz;
	printf("La multiplicacion resulto en: %d", mul);
}
